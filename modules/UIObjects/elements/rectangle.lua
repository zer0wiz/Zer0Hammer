
local rectangle = {}

function rectangle.create(options)
    options = options or {}
    
    local width = options.width or 200
    local height = options.height or 30
    local x = options.x or 0
    local y = options.y or 0
end